class FiniteGroup {
    constructor(generators) { 
        // implement shreier-sims here - group action by 
        
        this.elems = new Set();  
    }

    assertClosed(){ 
        this.elems.forEach()
    }

    groupMult(i, j ) {
        return Math.multiply(i,j); 
    } 
}