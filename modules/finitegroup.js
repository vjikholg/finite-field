import {Matrix} from "math.js"
class FiniteGroup {
    constructor(generators) { 
        // implement shreier-sims here - group action by conjugation basically 
        stabPt; 

        this.elems = new Set();  
    }

    assertClosed(){ 
        this.elems.forEach()
    }

    groupMult(i, j ) {
        return Math.multiply(i,j); 
    } 
}